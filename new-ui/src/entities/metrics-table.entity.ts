import type { DataTableEntity } from "@/components/data-table/entity";
import { z } from 'zod'

export const metricsSchema = z.object({
    
})

export const metricsTableEntity: DataTableEntity<> = {
    id: 'metricsTableEntity',
    columns: [
        {

        }
    ],

}